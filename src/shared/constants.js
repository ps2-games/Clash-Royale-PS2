const {width: SCREEN_WIDTH, height: SCREEN_HEIGHT} = Screen.getMode();

const PLAYER_ONE_PORT = 0;

const CARD_TYPE = {
    FEITICO: "feitico",
    TROPA: "tropa",
    EDIFICIO: "edificio"
};

const TOWER_TYPE = {
    PRINCIPE: "principe",
    REI: "rei"
};

export {
    PLAYER_ONE_PORT,
    CARD_TYPE,
    TOWER_TYPE,
    SCREEN_WIDTH,
    SCREEN_HEIGHT
}